streamlit>=1.52.1
openai-whisper>=20231117
librosa>=0.10.1
# pydub>=0.25.1  # Không cần nữa - đã thay bằng librosa/soundfile để tránh phụ thuộc ffprobe
soundfile>=0.12.1
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
python-docx>=1.1.0
reportlab>=4.0.0
scipy>=1.11.0
torch>=2.0.0
torchaudio>=2.0.0
imageio-ffmpeg>=0.5.0
sentencepiece>=0.1.99
protobuf>=4.25.0
transformers>=4.30.0
accelerate>=0.20.0
jiwer>=3.0.0
tf-keras>=2.15.0  # Cần cho PhoWhisper với Keras 3 compatibility
# Wav2Vec 2.0 (đã có transformers)
# datasets>=2.0.0  # Không cần thiết - wav2vec2 chỉ cần transformers/torch. Comment để tránh lỗi với Python 3.13
# API/Streaming
fastapi>=0.110.0
uvicorn>=0.23.0
audio-recorder-streamlit>=0.0.8
altair>=5.0.0
# Optional diarization/ASR backends (cài khi cần)
# pyannote.audio
# vosk
# deepspeech
# nemo-toolkit[asr]
# DeepSpeech 2 (optional)
# deepspeech>=0.10.0  # Uncomment nếu muốn sử dụng DeepSpeech
# NeMo/QuartzNet (optional)
# nemo-toolkit[asr]>=1.15.0  # Uncomment nếu muốn sử dụng QuartzNet
# Wav2Letter++ và Kaldi cần cài đặt thủ công

